@model AMS.Models.AssetViewModel.AssetCRUDViewModel

<div class="row">
    <table class="CustomBlueTable" id="tbllistAssetAssigned" style="width:100%;" border="1">
        <thead>
            <tr>
                <th>@Resource.Id</th>
                <th>@Resource.AssetName</th>
                <th>@Resource.EmployeeId</th>
                <th>@Resource.EmployeeName</th>
                <th>@Resource.Date</th>
                <th>@Resource.Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.AssetAssignedCRUDViewModel.listAssetAssignedCRUDViewModel)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.AssetName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EmployeeId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EmployeeName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CreatedDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Status)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>